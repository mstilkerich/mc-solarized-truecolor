[skin]
    256colors = true
    description = Solarized (mostly) dark skin for Midnight Commander.
    terminal_default_color = color244;color234

[Lines]
    # TODO: Description.
    horiz = ─
    vert = │
    lefttop = ┌
    righttop = ┐
    leftbottom = └
    rightbottom = ┘
    topmiddle = ─
    bottommiddle = ─
    leftmiddle = ├
    rightmiddle = ┤
    cross = ┼
    dhoriz = ═
    dvert = ║
    dlefttop = ╔
    drighttop = ╗
    dleftbottom = ╚
    drightbottom = ╝
    dtopmiddle = ╤
    dbottommiddle = ╧
    dleftmiddle = ╟
    drightmiddle = ╢

[core]
# Panes.
    _default_ = color254;color235                ;; Default file colors/background.
    selected = color230;color37                  ;; Color while hovering above a file/directory.
    marked  = color230;color61                   ;; Marked line (using select - insert, ctrl-t, etc...)
    markselect = color160;color37                ;; Marked and selected.
    header = color230                            ;; Pane header (name,size,date titles).
    reverse = color230;color61                   ;; The direcotry PWD above the header, in focused pane.

# Input fields.
    inputunchanged = color254;color235           ;; Input fields colors.
    input = color230;color235                    ;; Input fields, modified.
    inputhistory = color160;color254             ;; Input history controller.
    inputmark = color235;color166                ;; Input field marked text, if any.
    disabled = color245;color254                 ;; Disabled input fields in dialog boxes.
    gauge = color125;color33                     ;; Progress bars (while copying etc). TODO: What the text stands for?!

# Command line.
    # TODO: Command prompt line colors. What are they?!
    commandlinemark = color235;color166          ;; Commandline marked text, if any.
    commandhistory = color254;color235           ;; Command prompt history controller.

    # TODO: this color is missing in base theme:
    # shadow

[popupmenu]
# user menu, screenlist, encoding, etc...
    _default_ = color244;color234
    menusel = color244;color235
    menutitle = color245;color234

[dialog]
    _default_ = color235;color254                ;; Default colors of dialogs (e.g. copy dialog).
    dfocus = color235;color64                    ;; Controller colors (checkboxes, actions) in focus.
    dhotnormal = color160;color254               ;; Controllers hotkey color.
    dhotfocus = color160;color64                 ;; Controllers hotkey color in focus.
    dtitle = color230;color61                    ;; Dialog title.

[diffviewer]
    # TODO these colors are missing in base theme:
    # added
    # changed
    # changedline
    # changednew
    # error
    # removed

[error]
    _default_ = color235;color254                ;; Default colors of error dialogs (e.g. overwrite permission).
    errdfocus = color235;color64                 ;; Controller colors (checkboxes, actions) in focus.
    errdhotnormal = color160                     ;; Controllers hotkey color.
    errdhotfocus = color160;color64              ;; Controllers hotkey color in focus.
    errdtitle = color230;color160                ;; Error dialog title.

[filehighlight]
# View filehighlight.ini as reference.
    directory = color37;color235                 ;; Directories.
    executable = color166;color235               ;; Executable files.
    symlink = color125;color235                  ;; Symbolic links.
    stalelink = color254;color160                ;; Broken links.
    device = color160;color33                    ;; An interface for a device driver.
    special = color235;color33                   ;; Pipes (FIFOs), sockets, block files, character files etc...
    core = color245;color235                     ;; Core dump files, etc?...
    temp = color240;color235                     ;; Bak, Temp, ~, etc...
    archive = color61;color235                   ;; Archives.
    source = color64;color235                    ;; Source files.
    media = color136;color235                    ;; Interactive media.
    graph = color33;color235                     ;; Images, in other words.
    database = color254;color235                 ;; DataBases, duh.
    doc = color160;color235                      ;; Documents, duh.

[menu]
    _default_ = color235;color254                ;; Default colors for menu while active.
    menuinactive = color235;color254             ;; Colors for menu while inactive.
    menuhot = color160;color254                  ;; Menu item hotkey.
    menusel = color235;color64                   ;; Selected menu item.
    menuhotsel = color160;color64                ;; Selected menu item hotkey.

[statusbar]
    _default_ = color244;color235

[help]
    _default_ = color235;color254                ;; Default help tooltip text color/background.
    helpitalic = color61                         ;; Help when italic.
    helpbold = color160                          ;; Help when bold.
    helplink = color37                           ;; Links insde help tooltip.
    helpslink = color64                          ;; Links insde help tooltip focused.
    helptitle = color230;color61                 ;; Help title.

[buttonbar]
#Bottom button bar (copy,move, etc...)
    hotkey = color160;color234                   ;; Hotkey colors (1-10).
    button = color230;color234                   ;; Button colors (text itself).

[editor]
    _default_ = color244;color234                ;; Basic pane theme.
    editbold = color160;color235                 ;; Highlighted search results.
    editmarked = color254;color64                ;; Marked region.
    editwhitespace = color244;color235           ;; Tabs and trailing spaces.
    editlinestate = color244;color235            ;; Line numbers area (if enabled)
    bookmark = color230;color37                  ;; Bookmarked line.
    bookmarkfound = color235;color64             ;; Hover on bookmark(?).
    editbg = color244;color234                   ;; Basic editor theme (No full screen).
    editframe = color254;color61                 ;; Modified pane(?).
    editframeactive = color254;color235          ;; Active pane.
    editframedrag = color254;color64             ;; Moved/Resized pane.
    # TODO: this color is missing in base theme:
    # editrightmargin

[viewer]
    # TODO: These.
    _default_ = color244;color234
    viewunderline = color61;color235
    viewbold = color61;color235
    viewselected = color61;color235

[widget-common]
    # TODO: These.
    sort-sign-up = ↓
    sort-sign-down = ↑

[widget-panel]
    # TODO: These.
    hiddenfiles-sign-show = ⋅
    hiddenfiles-sign-hide = •
    history-prev-item-sign = «
    history-next-item-sign = »
    history-show-list-sign = ^

[widget-scollbar]
    # TODO: These.
    first-vert-char = ↑
    last-vert-char = ↓
    first-horiz-char = «
    last-horiz-char = »
    current-char = ■
    background-char = ▒
