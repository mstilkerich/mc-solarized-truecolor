[skin]
    256colors = true
    description = Solarized (mostly) dark skin for Midnight Commander.
    terminal_default_color = color241;color230;

[Lines]
    # TODO: Description.
    horiz = ─
    vert = │
    lefttop = ┌
    righttop = ┐
    leftbottom = └
    rightbottom = ┘
    topmiddle = ─
    bottommiddle = ─
    leftmiddle = ├
    rightmiddle = ┤
    cross = ┼
    dhoriz = ═
    dvert = ║
    dlefttop = ╔
    drighttop = ╗
    dleftbottom = ╚
    drightbottom = ╝
    dtopmiddle = ╤
    dbottommiddle = ╧
    dleftmiddle = ╟
    drightmiddle = ╢

# Color usage
#
# Foreground/background of "normal" content: fg/bg
# Headings of dialogs / popups: bold orange, background same as the other content
# Selection: reverse the normal colors
[core]
# Panes.
    _default_ = color241;color230;               ;; Default file colors/background.
    reverse = color166;color230;bold                     ;; The directory PWD above the header, in focused pane.
    header = color235;color230;bold                     ;; Pane header (name,size,date titles).
    selected = color230;color241;                ;; Color while hovering above a file/directory.
    marked  = color235;color61;                  ;; Marked line (using select - insert, ctrl-t, etc...)
    markselect = color235;color166;bold          ;; Marked and selected.

# Input fields.
    inputunchanged = color235;color254;          ;; Input fields colors.
    input = color234;color254;                   ;; Input fields, modified.
    inputhistory = color160;color235;            ;; Input history controller.
    inputmark = color254;color166;               ;; Input field marked text, if any.
    disabled = color240;color235;                ;; Disabled input fields in dialog boxes.
    gauge = color125;color33;                    ;; Progress bars (while copying etc). TODO: What the text stands for?!

# Command line.
    # TODO: Command prompt line colors. What are they?!
    commandlinemark = color254;color166;         ;; Commandline marked text, if any. (e.g. use Shift+LeftArrow to mark)
    commandhistory = color235;color254;          ;; Command prompt history controller.

    # TODO: this color is missing in base theme:
    # shadow

[menu]
    _default_ = color240;color254;               ;; Default colors for menu while active.
    menuinactive = color234;color254;bold               ;; Colors for menu while inactive.
    menuhot = color160;color254;                 ;; Menu item hotkey.
    menusel = color254;color240;                 ;; Selected menu item.
    menuhotsel = color160;color240;              ;; Selected menu item hotkey.

[popupmenu]
# user menu, screenlist, encoding, etc...
    _default_ = color240;color254;
    menusel = color254;color240;
    menutitle = color166;color254;bold

[dialog]
    _default_ = color240;color254;               ;; Default colors of dialogs (e.g. copy dialog).
    dfocus = color254;color240;                  ;; Controller colors (checkboxes, actions) in focus.
    dhotnormal = color160;color254;              ;; Controllers hotkey color.
    dhotfocus = color160;color240;               ;; Controllers hotkey color in focus.
    dtitle = color166;color254;bold                    ;; Dialog title.

[diffviewer]
    # TODO these colors are missing in base theme:
    # added
    # changed
    # changedline
    # changednew
    # error
    # removed

[error]
    _default_ = color254;color235;               ;; Default colors of error dialogs (e.g. overwrite permission).
    errdfocus = color254;color64;                ;; Controller colors (checkboxes, actions) in focus.
    errdhotnormal = color160;;                   ;; Controllers hotkey color.
    errdhotfocus = color160;color64;             ;; Controllers hotkey color in focus.
    errdtitle = color234;color160;               ;; Error dialog title.

[filehighlight]
# View filehighlight.ini as reference.
    directory = color33;;;;bold                       ;; Directories.
    executable = color64;;;;bold                     ;; Executable files.
    symlink = color37;;;;bold                         ;; Symbolic links.
    stalelink = color160;;;;bold+underline              ;; Broken links.
    device = color136;;;;bold+underline              ;; An interface for a device driver.
    special = color125;;;;bold                      ;; Pipes (FIFOs), sockets, block files, character files etc...
    core = color241;;                            ;; Core dump files, etc?...
    temp = color245;;                            ;; Bak, Temp, ~, etc...
    archive = color160;;;;bold                          ;; Archives.
    source = color241;;                          ;; Source files.
    media = color37;;                            ;; Interactive media.
    graph = color125;;                           ;; Images, in other words.
    database = color241;;                        ;; DataBases, duh.
    doc = color166;;                             ;; Documents, duh.

[statusbar]
    _default_ = color241;color254;

[help]
    _default_ = color254;color235;               ;; Default help tooltip text color/background.
    helpitalic = color61;;                       ;; Help when italic.
    helpbold = color160;;                        ;; Help when bold.
    helplink = color37;;                         ;; Links insde help tooltip.
    helpslink = color64;;                        ;; Links insde help tooltip focused.
    helptitle = color234;color61;                ;; Help title.

[buttonbar]
#Bottom button bar (copy,move, etc...)
    hotkey = color160;color254;bold               ;; Hotkey colors (1-10).
    button = color234;color254;bold                     ;; Button colors (text itself).

[editor]
    _default_ = color241;color230;               ;; Basic pane theme.
    editbold = color160;color254;                ;; Highlighted search results.
    editmarked = color235;color64;               ;; Marked region.
    editwhitespace = color241;color254;          ;; Tabs and trailing spaces.
    editlinestate = color241;color254;           ;; Line numbers area (if enabled)
    bookmark = color234;color37;                 ;; Bookmarked line.
    bookmarkfound = color254;color64;            ;; Hover on bookmark(?).
    editbg = color241;color230;                  ;; Basic editor theme (No full screen).
    editframe = color235;color61;                ;; Modified pane(?).
    editframeactive = color235;color254;         ;; Active pane.
    editframedrag = color235;color64;            ;; Moved/Resized pane.
    # TODO: this color is missing in base theme:
    # editrightmargin

[viewer]
    # TODO: These.
    _default_ = color241;color230;
    viewunderline = color61;color254;
    viewbold = color61;color254;
    viewselected = color61;color254;

[widget-common]
    # TODO: These.
    sort-sign-up = ↓
    sort-sign-down = ↑

[widget-panel]
    # TODO: These.
    hiddenfiles-sign-show = ⋅
    hiddenfiles-sign-hide = •
    history-prev-item-sign = «
    history-next-item-sign = »
    history-show-list-sign = ^

[widget-scollbar]
    # TODO: These.
    first-vert-char = ↑
    last-vert-char = ↓
    first-horiz-char = «
    last-horiz-char = »
    current-char = ■
    background-char = ▒
